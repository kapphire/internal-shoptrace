setup:
  addons:
    - plan: 'heroku-postgresql:hobby-basic'
      as: DATABASE
  config:
    DJANGO_SETTINGS_MODULE: interShoptrace.settings.staging
    SECRET_KEY: fl!3*dy@g%99b*#t5i+@zoc$yogoqk#col8i2fyeb&818144l&
run:
  release: python manage.py migrate
  web: gunicorn interShoptrace.wsgi
  celery_beat: celery beat -S redbeat.RedBeatScheduler -A interShoptrace.celeryapp:app --loglevel=debug
  celery_upload: celery worker -Q upload -A interShoptrace.celeryapp:app -n cdm_upload.%%h --loglevel=info  --without-gossip --without-mingle --without-heartbeat --concurrency 2 -Ofair --max-memory-per-child=512000
build:
  languages:
    - python
  packages:
    - gdal-bin